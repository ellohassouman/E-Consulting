import { TypeDocumentModel } from './type-document-model.model';

describe('TypeDocumentModel', () => {
  it('should create an instance', () => {
    expect(new TypeDocumentModel()).toBeTruthy();
  });
});
